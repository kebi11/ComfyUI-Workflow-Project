# EXP-XXX 实验记录

```text
实验编号：
日期：
目标：
对应问题：          P0X / 无
基于工作流：
测试工作流：
参考素材：
Seed：

修改内容：

修改前参数：

修改后参数：

保持不变的变量：

测试结果：
结果路径：

优点：

缺点：

是否改善：          是 / 部分 / 否 / 无法判断
结论：

下一步：
```

填写要求：

- 一次实验只声明一个主变量；多变量必须显式写出，且结论不得归因到单点
- `基于工作流` 必须是已存在的 baseline 或旧实验
- 结果图放到 `results/experiments/` 或 `results/comparison/`
- 写完后在 `logs/experiment-log/INDEX.md` 追加一行

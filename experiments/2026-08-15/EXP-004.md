# EXP-004 实验记录

```text
实验编号：        EXP-004
日期：            2026-08-15
目标：            验证 AutoClothes Phase 1：手工服装 Mask + Crop/Fooocus 换装，锁身材肤色
对应问题：        P01 腰臀漂移 / P04 6GB / 新 P06 旧衣残影
基于工作流：      workflows/baseline/2/WAI_Universal_Inpaint_v1.json
测试工作流：      workflows/experiments/WAI_AutoClothes_Inpaint_v1-phase1.json
参考素材：        references/character/gu_ref.jpg
                  references/clothing/gu_clothes_mask_hand_v1.png
Seed：            114514 / fixed

修改内容：
- 从 Universal Inpaint 派生，不覆盖 baseline
- 补齐 input/顾.jpg 别名；正式入口改用 gu_ref.jpg
- 把 clipspace 手绘 alpha 导出为独立服装 Mask（白=重绘）
- Mask 改为 LoadImage + ImageToMask，不再依赖右键涂抹
- Prompt 写目标服装，并加上肤色/体型约束
- 负向加入衣服残影、肤色和腰臀漂移

修改前参数：
Universal Inpaint：手绘 Mask 入口，黑裙 Prompt，LoadImage=顾.jpg（当时 input 中不存在）

修改后参数：
Mask 文件 gu_clothes_mask_hand_v1.png
Denoise 仍 0.95；Crop 仍 expand 14 / blend 16 / context 1.8 / 1024 / CPU

保持不变的变量：
WAI v1.7、Fooocus v26、KSampler 24/5/euler_ancestral/normal、Color Match=0

测试结果：
图已接好，JSON 连线校验通过。ComfyUI API 未启动，本条尚未出图。
结果路径：
results/experiments/EXP-004/（待放入）

优点：
- 输入图和 Mask 都可复现
- 身份锁定仍靠 Mask 外不进 VAE
- 为 Segformer 替换 Mask 留了单独入口

缺点：
- 仍依赖手工 Mask，不是自动认衣
- 现有 Mask 可能漏掉部分薄纱边，残影风险仍在
- 未实测 6GB 峰值

是否改善：        无法判断
结论：            Phase 1 主干在节点、权重、输入文件上已具备 Queue 条件。
                  先看单张换装是否锁脸/肤色/腿型，再决定装 LayerStyle。

下一步：
1. 重启 ComfyUI，加载本 JSON，Queue 一张
2. 若旧纱残留：只加大 Mask，不要先改 Prompt
3. 若腰变粗或肤色漂：记入本条，不要同时改 denoise
4. 通过后再装 ComfyUI_LayerStyle，做 Phase 2 自动认衣
```

## 本机 Queue 清单

打开：`workflows/experiments/WAI_AutoClothes_Inpaint_v1-phase1.json`

启动前确认：

- [ ] `input/gu_ref.jpg` 存在
- [ ] `input/gu_clothes_mask_hand_v1.png` 存在
- [ ] `models/inpaint/fooocus_inpaint_head.pth`（52 KB）
- [ ] `models/inpaint/inpaint_v26.fooocus.patch`（约 1.23 GB）
- [ ] 节点无 missing：`InpaintCropImproved` / `INPAINT_LoadFooocusInpaint`

出图后按项打勾（对原图看，不要凭感觉）：

- [ ] 脸、眼睛、草帽、发型主体无可见变化
- [ ] 已露出的手臂、腿、肤色无明显变深/变暖
- [ ] 腰宽、腿长视觉比例不明显变粗变短
- [ ] 薄纱/系结被去掉，没有灰块
- [ ] 背景和脚上凉鞋基本不动
- [ ] 6GB 没有 OOM

失败回退：

```text
OOM            → Crop target 1024 改 896
灰块 / 旧纱    → 先补 Mask，再试 denoise 1.00
脸被改         → Mask 吃到头发或下巴，缩小白区
腰臀漂         → 记下，不要立刻加 IPAdapter
```

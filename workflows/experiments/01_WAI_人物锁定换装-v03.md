# 01_WAI_人物锁定换装-v03

对应 JSON：`01_WAI_人物锁定换装-v03.json`  
设计说明书：`../baseline/1/方案一 v3：基于当前工作流节点编号的 3 个定点修复分支开发文档.md`  
派生自：`01_WAI_人物锁定换装-v02.json`（不覆盖）

状态：**已建成，未实测**。

## 这张图做什么

v2 主流程（到 Node 41）之后，再串三个**定点修复**，不再整衣重修：

```text
41 v2成品
 ↓
A 结构修复（清旧纱残影）   denoise 0.22
 ↓
B 腰线融合                 denoise 0.12
 ↓
C 裙摆接缝                 denoise 0.12
 ↓
60  01_WAI_cloth_lock_v3
```

## 三个局部 Mask

| 节点 | 名称 | 只涂 |
|---|---|---|
| 42 | Structure Fix Mask | 左腰 / 左大腿白残影、旧透明纱，不要整裙 |
| 48 | Waist Blend Mask | 上衣下摆 + 高腰 + 裙腰上缘 |
| 54 | Hem Seam Mask | 裙摆下缘、裙-腿、裙-背景边 |

画法：黑底亮白，不要开负面。某张不涂（全黑）= 这一支自动跳过。

三张 Mask 都先从 `顾.jpg` 打开。若发现 Mask 互相覆盖，把原图复制成三个文件再分别画。

## 中间结果

- `01_WAI_v3_A`：A 后
- `01_WAI_v3_B`：B 后
- `01_WAI_cloth_lock_v3`：最终
- v2 的 `01_WAI_v2_lock` 仍会保存，方便对照

## 出问题怎么退

| 现象 | 处理 |
|---|---|
| A 后裙子形状坏了 | 缩小 A Mask，0.22→0.20 |
| B 后腰线发糊 | 缩小 B Mask，0.12→0.10 |
| C 后裙边更假 | 缩窄 C Mask，保持低 denoise |
| 头发被改到 | 把发丝从当前 Mask 里擦掉 |

本轮这张图建议：A 必跑，C 必跑，B 建议跑。
